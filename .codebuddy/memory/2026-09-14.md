# 2026-09-14

## spec-select-popup 改造（规格选择弹层）

需求：弹层高度固定 713px，超出滚动；去制作按钮悬浮在弹层底部（bottom: 38px，水平居中）。

改动文件：
- `src/components/spec-select-popup/index.tsx`
  - 引入 `ScrollView`，把原本的 `<View className='px-[12px]'>` 内容区改为 `<ScrollView className='spec-popup-scroll' scrollY>` 包裹，内层 `View` 增加 `pb-[110px]`（给悬浮按钮留位）。
  - 价格说明文案去掉 `mb-[48px]`（底部留白改由滚动区 padding 承担）。
  - 去制作按钮（登录/未登录两种态）从内容流中移出，改为独立 `<View className='spec-popup-footer'>`，移除原来的 `marginBottom: max(env(safe-area-inset-bottom), 34px)` 内联样式，登录态 `View` 补 `w-full` 保证水平居中撑满。
- `src/components/spec-select-popup/index.scss`
  - `.spec-select-popup { height: 713px; overflow: hidden; }`
  - 新增 `.spec-popup-scroll { height: 100%; }`
  - 新增 `.spec-popup-footer { position: absolute; left/right: 0; bottom: 38px; padding: 0 12px; z-index: 10; }`（绝对定位相对 `.nut-popup`，因为 NutUI popup 根节点是 `position: fixed`）

校验：`npx eslint` 通过；`npx tsc --noEmit` 无本文件报错；`npx stylelint` 仅剩改动前已有的 3 处告警（nut- 类名 kebab-case、空行）。

## 追加：去制作按钮宽度固定 310px
- tsx：登录态 `View` 与未登录 `Button` 的 `w-full` 改为 `w-[310px]`（310 设计稿 px，构建后 620rpx）。
- scss：`.spec-popup-footer` 增加 `display: flex; justify-content: center;` 保持水平居中。
