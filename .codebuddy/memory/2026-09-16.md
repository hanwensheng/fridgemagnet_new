# 2026-09-16

## 订单详情：实付款下新增「优惠」行
需求：订单详情「实付款」下方新增优惠展示列；左「优惠」，右为 `goodsPrice - orderPrice`（形如 `-¥ 35.60`），无优惠显示 `-¥ 0.00`；红色 `#F2330D` / 12px / 600；带红色箭头 `icon_red_right.svg`（16×16），减 0 时不显示箭头。
- `src/pages-sub/order-detail/index.logic.ts`：新增 `discountAmount` memo = `Number(goodsPrice) - Number(orderPrice)`，仅在 `Number.isFinite(diff) && diff > 0` 时取 `toFixed(2)`，否则 0（防 NaN）；加入 hook 返回。
- `src/pages-sub/order-detail/index.tsx`：`import IconRedRight from '@/assets/svgs/icon_red_right.svg'`，在实付款行后插入优惠行（`order-detail-discount-wrap` 包住金额 + 条件箭头）。
- `src/pages-sub/order-detail/index.scss`：新增 `.order-detail-discount-wrap`（flex 居中）、`.order-detail-discount`（12px / #f2330d / 600）、`.order-detail-discount-arrow`（16×16，margin-left 2px）。
- 图标本身 `fill="#F2330D"`（红色），无需额外染色。
- 假设（已在回复中说明）：左侧「优惠」沿用该卡片既有标签样式（灰色 12px），红色只作用于右侧金额；金额用 `toFixed(2)`（与确认页一致，用户示例写的是 35.6 一位小数）。
- 箭头当前只是图标、不可点击；如需点击跳转/弹明细待用户指定。
- 校验：eslint / tsc 对本文件无**新增**错误。既有问题：`index.tsx` 解构出的 `estimatedShipText` 未被使用（用它的 JSX 被注释掉了）→ TS6133 + eslint warning，改动前就存在。

## 订单详情：点箭头弹出优惠明细（已完成）
需求：有优惠时点红色箭头弹优惠明细弹层，展示与确认订单页一致，底部换成 301×56 / #1C1C1E / radius 100 / 带阴影 / 16px 700 白字的「确认」按钮。
实现：
- **抽公共计算**：新增 `src/utils/discount.ts` —— `DISCOUNT_RATE = 0.8` + `calcDiscountedPrices(originalPrices): {originalPrice, price, discountAmount, discounted}[]`（最低价那件原价、并列取第一个）。确认订单页 `index.logic.ts` 的 `buildOrderItems` 改为调用它（原文件内 `DISCOUNT_RATE` 常量已删除），保证两页“计算方法一样”。
- **弹层复用**：`CouponDetailPopup` 新增 `footerType?: 'pay' | 'confirm'`（默认 pay）+ `onConfirm?`，pay 相关 props 改为可选并给默认值（确认订单页调用处无需改动）；confirm 模式底部渲染 `.coupon-detail-confirm-btn`（301×56、radius 100、`#1c1c1e`、`box-shadow: 0 8px 10px -6px rgba(0,0,0,.1), 0 20px 25px -5px rgba(0,0,0,.1)`、白字 16px/700 居中），`footer--confirm` 用 `justify-content: center` 覆盖 space-between。点击 = 关闭弹层（`onClose()` + `onConfirm?.()`）。
- **订单详情逻辑**：`goodsPrices`（静默拉 `getGoodsList(undefined, {showLoading:false, showError:false})` 组 goodsId→price）、`discountPopupItems`（图片/宽高取 `order.imgList`，价格为 0 或取不到时用「均价 = goodsPrice / max(goodsNum,1)」兜底）、`discountPopupVisible` + `open/closeDiscountPopup`。
  - **注意**：`OrderImgItem` 只有 `goodsId/imgLink/width/height/pkId`，**没有价格**，所以价格必须按 goodsId 从商品列表接口取。
- **TSX**：优惠行金额+箭头外层 `order-detail-discount-wrap` 在 `discountAmount > 0` 时 `onClick={openDiscountPopup}`；`BasePage` 内渲染 `<CouponDetailPopup visible items footerType='confirm' onClose />`。
- 数值验证：3 件 39+39+139 → goodsPrice 217 − orderPrice 181.4 = **35.6**（与用户给的示例一致）；弹层逐件减免 7.8+27.8 = 35.6 与行上金额一致。
- 待确认：确认按钮文案用的是「确认」（用户未指定）；点击仅关闭弹层。
- 校验：eslint / tsc 无新增错误。

### 更正：订单 imgList 里其实有 price（用户指出）
- `OrderImgItem` 类型原先没声明 `price`，但接口实际返回有 → 已补 `price?: number`（注释：下单时的商品原价）。
- 取价优先级改为：`imgList[].price` → 商品列表接口价（按 goodsId）→ 商品均价（`goodsPrice / max(goodsNum,1)`）。
- 新增自检 `console.warn`：`Σ imgList.price` 与 `order.goodsPrice` 相差 > 0.01 时告警（用于验证 imgList.price 是原价而不是折后价）。
- 按钮文案与行为已确认为「确认」+ 点击关闭弹层，无需改动。
- 商品列表接口那次静默请求现在只作兜底，若确认 imgList.price 一定存在可删除。

### review 结论（9/16 全部改动）
- 今天改动文件：`utils/discount.ts`(新)、`coupon-detail-popup/{index.tsx,index.scss}`、`order-confirm/index.logic.ts`、`order-detail/{index.tsx,index.logic.ts,index.scss}`、`api/modules/order.ts`。
- **发现并修复 1 个真问题**：弹层列表 `key={item.id}` 可能重复（同一规格下单多次 → id 相同；订单 imgList 里同一 goodsId 也可出现两次）→ 改为 `key={\`${item.id}-${index}\`}`（同时修好确认页弹层的潜在重复 key）。
- 待留意：详情弹层价格完全依赖 `imgList[].price`，老订单缺该字段时退到“商品均价”（每件同价 → 除第一件外全 8 折），数值会不准 → 已挂自检 warn，需看老订单控制台。
- 小问题（未改）：`OrderItem.discountTag` 现在只是“是否打折”标记（值 '8折' 不再展示）；`Number(img.price) || avgPrice` 会把合法 0 价当缺失；confirm 模式无总额兜底/空列表无提示；`estimatedShipText` TS6133（既有）。
- 提交前注意（非今天改动）：`src/api/request.ts` dev BASE_URL 指向测试服 IP、两个 project 配置 libVersion=3.17.2、package-lock 变动。
- 验证手段：eslint + tsc（仅剩既有 TS6133）+ node 数值模拟；**未跑真机/构建**。

### 追加：删掉商品列表静默请求（用户确认“删掉吧”）
`src/pages-sub/order-detail/index.logic.ts`：删除 `getGoodsList` 的 useEffect、`goodsPrices` state、`productApi` import，`discountPopupItems` 依赖改回 `[order]`。价格链简化为 **`imgList[].price` → 商品均价（`goodsPrice / max(goodsNum,1)`）**。自检 `console.warn`（Σprice vs goodsPrice）保留。eslint / tsc 无新增错误。
